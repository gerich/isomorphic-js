import React, { PropTypes, Component } from 'react'

import './App.css'

const propTypes = {
  initialName: PropTypes.string
};

const defaultProps = {
  initialName: 'Аноним'
};

class App extends Component {

}
